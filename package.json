{
  "name": "umair-quiz-app",
  "version": "1.0.0",
  "description": "Educational quiz application with AI-powered content generation",
  "main": "backend/server.js",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build"
  },
  "keywords": [
    "quiz",
    "education",
    "ai",
    "openai"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "dotenv": "^17.2.2",
    "openai": "^5.19.1"
  },
  "devDependencies": {
    "pm2": "^5.3.0"
  }
}
